# Agent Guide: Task Manager Source

## Scope

Applies to Rust source for Task Manager API.

## Feature Notes

- Task CRUD handler logic lives in `handlers.rs`.
- Shared in-memory state lives in `models.rs`.
- HTTP error mapping lives in `error.rs`.
- Keep route paths stable unless docs and README change too.

